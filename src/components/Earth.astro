<div class="earth">
    <img src="/continents.svg" alt="earth's continents" />
</div>
<div class="atmosphere"></div>

<style>
    .earth,
    .atmosphere {
        position: absolute;
        left: 0;
        aspect-ratio: 1;
        transform: translateX(-50%);
        border-radius: 50%;
    }

    .earth,
    .atmosphere {
        overflow: hidden;
        box-shadow: var(--shadow-6);
        z-index: 1;
    }

    .earth {
        top: -22.5vh;
        background: var(--nord9-frost);
        width: 45vh;
        z-index: 2;
        box-shadow: var(--shadow-6);
    }

    .earth img {
        width: 100%;
        height: 100%;
        display: block;
        transform: rotate(15deg) translate(5%, 32%);
    }

    .atmosphere {
        top: -25vh;
        width: 50vh;
        height: 50vh;
        background: var(--nord8-frost);
        z-index: 1;
        box-shadow: var(--shadow-8);
    }
</style>
