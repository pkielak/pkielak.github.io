<div class="curvedTextWrapper">
    <div class="curvedText">
        <svg
            viewBox="0 0 220 220"
            aria-hidden="true"
            width="100%"
            height="100%"
            role="img"
            preserveAspectRatio="xMidYMid meet"
        >
            <defs>
                <path
                    id="circlePath"
                    d="M110,110 m-100,0 a100,100 0 1,1 200,0 a100,100 0 1,1 -200,0"
                ></path>
            </defs>
            <text
                font-size="14"
                fill="#d8dee9"
                font-family="'IBM Plex Mono', monospace"
            >
                <textPath
                    href="#circlePath"
                    startOffset="50%"
                    text-anchor="middle"
                >
                    {
                        "JavaScript - - TypeScript - - CSS - - React - - Astro - - Nextjs - - Postgres - - AI Agents - -"
                    }
                </textPath>
            </text>
        </svg>
    </div>
</div>

<style>
    .planet .curvedTextWrapper {
        position: absolute;
        top: 0;
        left: 0;
        width: 120%;
        aspect-ratio: 1;
        z-index: 99;
        user-select: none;
        pointer-events: none;
    }

    .curvedText {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 120%;
        aspect-ratio: 1;
        transform: translate(-50%, -50%) rotate(0deg);
        z-index: 99;
        animation: rotate 16s linear infinite;
    }

    @keyframes rotate {
        100% {
            transform: translate(-50%, -50%) rotate(360deg);
        }
    }
</style>
